{
  "name": "@libral/transport-service",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node ./dist/server.js",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && node ./dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.19.2",
    "yaml": "^2.4.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}